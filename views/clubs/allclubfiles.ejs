<% include ../header %>

<% if(club["links"].length > 0){ %>
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>Student</th>
        <th>Form</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% for(var i = 0; i < club["links"].length; i++) {%>
        <tr>
            <td>
              <%= club["links"][i]["userID"] %>
            </td>
            <td>
              <%= club["links"][i]["link"] %>
            </td>
        </tr>
      <% } %>
    </tbody>
  </table>
<% } else { %>
  <p><%= club["clubname"] %> does not currently have any uploaded files!</p>
<% } %>

<% include ../footer %>